---
const { id, class: klass = '', ...rest } = Astro.props;
---

<h2 id={id} {...rest} class={`film-section-title font-medium ${klass}`}>
  <slot />
</h2>

<style>
  .film-section-title {
    position: relative;
    margin-top: 3.5rem;
    padding-top: 1.25rem;
  }

  .film-section-title::before {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    height: 1px;
    background: rgba(255, 255, 255, 0.10);
  }

  /* first section: no divider */
  #film-content > .film-section-title {
    margin-top: 1.5rem;
    padding-top: 0;
  }
  #film-content > .film-section-title::before {
    display: none;
  }

  /* Avoid double divider: if a section title immediately follows the tabs nav, don't draw its divider */
#section-tabs + .film-section-title::before {
  display: none;
}

#section-tabs + .film-section-title {
  padding-top: 0;
  margin-top: 2.25rem; /* still some air below tabs */
}

</style>
